import { createSlice } from '@reduxjs/toolkit';

const darkMode = createSlice({
  name: 'darkMode',
  initialState: {},
  reducers: {}
});

export default darkMode.reducer;

export const {} = darkMode.actions;
